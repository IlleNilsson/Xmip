name: Rust

on:
  push:
    branches: [main, build-evaluation]
  pull_request:
    branches: [main]

jobs:
  workspace:
    name: Workspace evaluation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Download dependencies
        run: cargo fetch --locked

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Check workspace
        run: cargo check --workspace --all-targets --all-features --locked

      - name: Test workspace
        run: cargo test --workspace --all-features --locked

      - name: Build workspace
        run: cargo build --workspace --all-features --locked

  profiles:
    name: Profile checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: IoT profile check
        run: cargo check --workspace --no-default-features --features iot-profile --locked

      - name: Embedded profile check
        run: cargo check --workspace --no-default-features --features embedded-profile --locked
