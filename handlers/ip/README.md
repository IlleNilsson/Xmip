# IP Handler Family

This folder groups handlers rooted in IP networking.

IP is the logical root for TCP and UDP based handler technologies.

Sub-repository placeholders under this folder should become Git submodules or package references when the handler repositories exist.
