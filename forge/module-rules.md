# Module Rules

A repository is allowed when it represents a stable module boundary.

Use family repositories for broad technology areas.

Use implementation repositories for concrete handlers.

Use Cargo features for runtime selection.

Use static binary builds for IoT, edge and embedded profiles.

Use workspace and dynamic module options for server and larger client profiles.
