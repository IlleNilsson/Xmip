# Xmip Main Runtime

This path is reserved for the main runtime sub-repository.

The main runtime owns:

- kernel,
- runtime execution,
- persistence boundary,
- management boundary,
- module loading,
- handler registry,
- subscription evaluation,
- process execution,
- send port execution.

In the final repository layout this path should be replaced by a Git submodule or package reference to the main runtime repository.
